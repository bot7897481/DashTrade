# DashTrade Environment Configuration
# Copy this file to .env and fill in your values

# PostgreSQL Database Connection
# Format: postgresql://username:password@host:port/database
# Example: postgresql://user:pass@localhost:5432/dashtrade
DATABASE_URL=postgresql://username:password@localhost:5432/dashtrade

# Alpha Vantage API Key (Optional - for real-time data)
# Get your free API key from: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=your_api_key_here

# Encryption Key (REQUIRED for Trading Bot - Alpaca API keys encryption)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANT: Generate once and keep it secret! Losing this key means all encrypted API keys become unreadable.
ENCRYPTION_KEY=your_generated_fernet_key_here

# Webhook Base URL for TradingView (REQUIRED for trading bot signals)
# Railway: Usually auto-detected via RAILWAY_PUBLIC_DOMAIN
# If not auto-detected, set manually:
#   - Railway: https://your-app-name.up.railway.app
#   - Replit: https://your-repl.repl.co
# IMPORTANT: Must be publicly accessible (not localhost) for TradingView to reach it
WEBHOOK_BASE_URL=https://your-app.up.railway.app

# Application Settings (Optional)
# Uncomment to customize
# APP_PORT=5000
# APP_HOST=0.0.0.0

# Security Settings (Production)
# SESSION_SECRET=your_random_secret_key_here
# FORCE_HTTPS=true
